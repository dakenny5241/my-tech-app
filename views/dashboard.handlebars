<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard</title>
  </head>
  <body>
    <h1>Dashboard</h1>

    {{#each posts}}
      <h2>{{title}}</h2>
      <p>{{date}}</p>
    {{/each}}

    <a href="/post/new">Add New Post</a>
  </body>
</html>